name: Build and Test

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    name: Build and Test
    runs-on: ubuntu-latest
    container:
      image: nvidia/cuda:11.8.0-devel-ubuntu22.04

    steps:
    - name: Checkout code
      uses: actions/checkout@v4

    - name: Install dependencies
      run: |
        apt-get update
        DEBIAN_FRONTEND=noninteractive apt-get install -y \
          cmake \
          build-essential \
          zlib1g-dev \
          libomp-dev \
          file \
          git

    - name: Configure CMake
      run: |
        mkdir -p build
        cd build
        cmake .. -DCMAKE_BUILD_TYPE=Release \
          -DENABLE_FETCH_TINYXML2=ON \
          -DENABLE_FETCH_LIBDEFLATE=ON

    - name: Build
      run: |
        cd build
        make -j$(nproc)

    - name: Run unit tests
      run: |
        cd build
        ctest --output-on-failure

    - name: Verify binary
      run: |
        ls -lh build/cuda-demux
        file build/cuda-demux
        ldd build/cuda-demux || true
        # libdeflate is linked statically, so it must not appear here.
        ! ldd build/cuda-demux | grep -q libdeflate

    - name: Run basic test
      run: |
        build/cuda-demux --help || true